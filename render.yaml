services:
  - type: web
    name: yt-music-api
    env: node
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg python3 python3-pip
      pip3 install --upgrade yt-dlp
      npm install
    startCommand: node server.js
